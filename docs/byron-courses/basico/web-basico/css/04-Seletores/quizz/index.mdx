---
title: "Quiz"
sidebar_position: 10
---

import Quiz from "@site/src/components/Quiz";

Responda sem consultar o material.

## Perguntas

<Quiz
  question="1. Qual é a principal diferença entre o seletor filho e o seletor descendente?"
  answers={[
    "O seletor filho aplica estilos apenas aos elementos filhos diretos de um elemento pai, enquanto o seletor descendente aplica estilos a todos os elementos que são descendentes de um elemento pai, independentemente do nível hierárquico",
    "O seletor filho aplica estilos a todos os elementos dentro de um elemento pai, enquanto o seletor descendente aplica estilos apenas aos elementos netos de um elemento pai",
    "O seletor filho aplica estilos apenas aos elementos que são irmãos diretos de um determinado elemento, enquanto o seletor descendente aplica estilos a todos os elementos irmãos de um determinado elemento",
    "O seletor filho aplica estilos aos elementos que estão no mesmo nível hierárquico, enquanto o seletor descendente aplica estilos apenas aos primeiros filhos de um elemento pai",
  ]}
  correctIndex={0}
/>

<Quiz
  question="2. Quando é mais apropriado utilizar o seletor de ID?"
  answers={[
    "Quando você quer aplicar estilos a vários elementos diferentes na mesma página",
    "Quando você quer aplicar estilos a um elemento específico que deve ser único dentro de um documento HTML",
    "Quando você quer aplicar estilos a todos os elementos de um determinado tipo em uma página",
    "Quando você quer aplicar estilos a elementos que compartilham a mesma classe",
  ]}
  correctIndex={1}
/>

<Quiz
  question="3. Qual das alternativas abaixo descreve corretamente o que é um seletor de tipo em CSS?"
  answers={[
    "Um seletor que aplica estilos a elementos baseados em suas classes",
    "Um seletor que aplica estilos a elementos baseados em seus IDs",
    "Um seletor que aplica estilos a elementos HTML específicos, como <p>, <h1>, ou <div>",
    "Um seletor que aplica estilos a elementos baseados em seus atributos",
  ]}
  correctIndex={2}
/>

<Quiz
  question="4. Qual das seguintes declarações CSS utiliza um seletor de classe para estilizar elementos?"
  answers={[
    "#main { background-color: yellow; }",
    "button { padding: 10px; }",
    ".highlight { color: green; }",
    "h1 + p { margin-top: 20px; }",
  ]}
  correctIndex={2}
/>

<Quiz
  question="5. Qual das alternativas abaixo descreve corretamente o que é um seletor irmão adjacente em CSS?"
  answers={[
    "Um seletor que aplica estilos a um elemento baseado em sua localização geral no documento",
    "Um seletor que aplica estilos a um elemento que segue imediatamente outro elemento específico",
    "Um seletor que aplica estilos a todos os elementos que compartilham a mesma classe",
    "Um seletor que aplica estilos a todos os elementos que compartilham o mesmo ID",
  ]}
  correctIndex={1}
/>

<Quiz
  question="6. Qual das alternativas abaixo descreve corretamente o que é um seletor universal em CSS?"
  answers={[
    "Um seletor que aplica estilos a elementos HTML específicos, como <p>, <h1>, ou <div>",
    "Um seletor que aplica estilos a todos os elementos dentro de um contêiner específico",
    "Um seletor que aplica estilos a elementos baseados em seus IDs",
    "Um seletor que aplica estilos a todos os elementos de um documento HTML",
  ]}
  correctIndex={3}
/>
